node_modules/
dist/
target/
src-tauri/target/
tasks/
.DS_Store
*.swp
*.swo
.env
.env.*

# macOS code-signing secrets — never commit
src-tauri/.env.signing
*.p8
*.p12
*.cer
*.certSigningRequest

# Claude Code local state (settings, scheduled task locks, etc.)
.claude/

# Local plan-mode scratch
.plans/
