{
  "identifier": "default",
  "description": "Default capabilities for Launchpad",
  "windows": ["main", "window-*"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "shell:allow-open"
  ]
}
